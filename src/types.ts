type beast = {
    id: number,
    name: string,
    type: string,
    desc: string,
    loot: string|null,
    location: string|null,
    weakness: string|null,
    image: string
}

export default beast;
